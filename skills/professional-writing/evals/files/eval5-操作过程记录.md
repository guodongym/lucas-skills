# 测试环境服务发布记录

1. 我先连公司 VPN，确认内网能打开，然后 ssh 到跳板机。
2. 我从跳板机再连到测试环境的应用机，用的是值班群里发我的账号。
3. 我进到服务目录看了一下当前版本，记得显示的是上周的构建号。
4. 然后我把发布包传上去，文件名里带这次需求的编号，上传后跑了校验，输出显示 OK。
5. 接着我先停了旧进程，命令返回以后看进程列表，旧进程已经没有了。
6. 我把原来的目录复制了一份做备份，备份目录后面加了当天日期。
7. 然后改了下那个配置，把测试环境要用的值换进去，但具体是哪个文件我当时没记。
8. 启动的时候报了个连接超时，我顺手调了下那边的参数，后来重启一次就好了，具体参数名需要再查操作记录。
9. 重启后我看日志里出现了 `Application started`，再访问健康检查接口，返回状态是 `UP`。
10. 最后我从页面提交了一笔测试请求，后台日志里能查到对应请求编号，页面也显示处理成功。
